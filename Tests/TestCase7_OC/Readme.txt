this test was done on the pc in the OC room. 
TimeLimit=200s
For each pb instance we have launched: MIP, H1,H2,Preprocessing without MIP.

In the result, if the instance is infeasible for MIP, then we skip the H1,H2 and preprocessing.